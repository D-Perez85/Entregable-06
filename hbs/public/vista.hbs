<style>
    .img{
      width: 70px;
      height: 70px;
      padding: 0;
    }
    .image{
          width: 40px;
          height: 40px;
    }
</style>

<div class="border p-2">
    <h3 class="text-primary">Vista de Productos</h3>
    <br>
    {{#if products.length}}
         <table class="table">
          <tr> 
            <th class="font-weight-bold">Title</th> 
            <th class="font-weight-bold">Price</th> 
            <th class="font-weight-bold">Foto URL</th> 
          </tr>
            {{#each products}}
            <tr class="border-b-[1px] border-gray-200 cursor-pointer transition-colors hover:bg-gray-50">
              <td>{{this.title}}</td>
              <td>{{this.price}}</td>
              <td class="img"><img src={{this.thumbnail}} alt={{this.title}} class="image"/></td>
            </tr>
            {{/each}}
        </table>
     {{else}}
        <h3 class="alert alert-warning">No se encontraron productos</h3>

    {{/if}}
   